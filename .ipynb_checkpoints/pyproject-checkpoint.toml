[project]
name = "sandbox"
version = "0.1.0"
description = "Sandbox for MCP"
readme = "readme.md"
requires-python = "==3.12.*"
dependencies = [
    "numpy",
    "sympy",
    "scipy",
    "fastmcp",
    "mcp",
    "pytest",
    "vllm"
]
